# S3 directory bucket example

Creates an S3 Express One Zone directory bucket in a caller-selected Availability Zone or Local Zone. Supply a location-specific directory bucket name and the matching zone ID for the selected Region.

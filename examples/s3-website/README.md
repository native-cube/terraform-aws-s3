# S3 website example

Creates an encrypted bucket with S3 website configuration. Public access remains blocked by default, so use CloudFront with origin access control or deliberately supply a tightly scoped public policy before expecting direct public website access.

# S3 access point example

Creates an encrypted bucket and a VPC-scoped access point with public access blocked. Supply an existing VPC ID and add a least-privilege access point policy when your application requires one.

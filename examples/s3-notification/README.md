# S3 notification example

Creates a bucket with EventBridge plus Lambda, SNS, and SQS notification destinations. Supply existing destination ARNs and configure the corresponding Lambda permission, topic policy, and queue policy before applying.

# S3 replication example

Creates a versioned source bucket and configures replication, including existing-object replication, to an existing versioned destination bucket. Supply a destination bucket ARN and an IAM role whose trust and permissions allow Amazon S3 replication.
